[workspace]
resolver = "2"
members = [
    "crates/tts-spec-model",
    "crates/tts-spec-script",
    "crates/tts-spec-layout",
]

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/softwarewrighter/alltalk-client-rs"

[workspace.dependencies]
# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1.0"

# Error handling
thiserror = "1.0"

# Internal crates
tts-spec-model = { path = "crates/tts-spec-model" }
tts-spec-script = { path = "crates/tts-spec-script" }
tts-spec-layout = { path = "crates/tts-spec-layout" }
