[workspace]
resolver = "2"
members = [
    "tts-spec-model",
    "tts-spec-script",
    "tts-spec-layout",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/softwarewrighter/alltalk-client-rs"

[workspace.dependencies]
# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
serde_json = "1.0"

# Error handling
thiserror = "1.0"

# Internal crates
tts-spec-model = { path = "tts-spec-model" }
tts-spec-script = { path = "tts-spec-script" }
tts-spec-layout = { path = "tts-spec-layout" }
